pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        maven { url = uri("https://maven.neoforged.net/releases") }
    }
}

rootProject.name = "neo-youth-core"
